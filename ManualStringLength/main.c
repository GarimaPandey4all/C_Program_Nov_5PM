#include <stdio.h>
#include <stdlib.h>

int main()
{
    char name[10] = "Mumbai";
    int i;

    for(i = 0; name[i]!='\0'; i++)
    {

    }

    printf("Length of the String is: %d", i);

    return 0;
}
